<div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @infos.each do |info| %>
    <div class="content_post" style="background-image: url(<%= info.image %>);">
      <p><%= info.text %></p>
      <span class="name"><%= info.fish_name %></span>
    </div>
  <% end %>
</div>